/**
 * Application constants
 * 
 * Note: Folder and bookmark data is now stored in SQLite database.
 * See src/database/seed.js for initial seed data.
 */

// CSS/Layout constants
export const PAGE_MARGIN = 30;